{
  "name": "fake-json-api",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "json-server": "^0.17.4"
  },
  "scripts": {
    "start": "json-server --watch db.json --host 0.0.0.0 --port $PORT"
  }
}
